import { Box, Button, Checkbox, Container, Divider, Flex, FlexProps, FormControl, FormLabel, Heading, HStack, Input, Stack, Text, useBreakpointValue, useColorModeValue } from '@chakra-ui/react'
import { Logo } from './Logo'
import { OAuthButtonGroup } from './OAuthButtonGroup'
import { PasswordField } from './PasswordField'

export const Content = (props: FlexProps) => {
  return (
    <p>helo</p>
  )
}